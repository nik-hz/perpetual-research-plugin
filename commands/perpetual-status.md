---
description: Show current research status — experiments, hypotheses, runs, budget
user_invocable: true
---

Run `perpetual status` to show the current research state, then summarize the output for me. Highlight anything that needs attention (crashed runs, stale experiments, budget warnings).
