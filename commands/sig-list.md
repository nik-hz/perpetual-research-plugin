---
description: List all tracked symbols and their drift status
---

Run `sig list` and summarize the output. Flag drifted entries explicitly.

If the user passes an argument matching a substring, filter the symbol ids to those that contain it.
